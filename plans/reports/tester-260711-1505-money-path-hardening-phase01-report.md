# QA Report: Money Path Hardening Phase 01

**Date:** 2026-07-11 | **Tests:** 105/105 passed | **Analyze:** 0 issues

## Test Results

**Total:** 105 tests executed | **Passed:** 105 | **Failed:** 0 | **Skipped:** 0

Execution time: 17s

## New Test Coverage

### Test: "applies server-computed voucher discount to the paid amount"

**Location:** `FE/test/blocs/checkout_bloc_test.dart:146–215`

**Contract Locked:** ✓ STRONG

The test meaningfully exercises the promoCode contract:

1. **Mock wiring is sound:**
   - `CheckoutPlaceOrder` event properly threads `promoCode: 'SALE10'` to the bloc
   - Bloc forwards promoCode to `createOrder()` call (line 98 of checkout_bloc.dart)
   - Mock captures promoCode via explicit expectation (line 162 of test): `expect(promoCode, 'SALE10')`

2. **Server authority chain verified:**
   - Mock returns server-computed values: `subtotal: 100000`, `discountAmount: 10000`, `total: 105000` (105000 ≠ 100000 + 15000 shipping)
   - Bloc extracts `final total = created.total` (line 100 of checkout_bloc.dart) — not recomputing client-side
   - Bloc passes extracted total to `_createPaymentRow()` (line 119): `amount: total`
   - Test asserts payment amount is 105000.0 (line 211) — server total, not naive 115000

3. **No weak assertions:**
   - Assertion on promoCode passthrough is explicit, not implicit
   - Assertion on payment amount flow verifies the exact server-returned value
   - State transition expectations (isLoading, isSuccess, orderId) are correct

4. **Test isolation is clean:**
   - No side effects from other tests (mocks instantiated fresh per test)
   - Mock setup and assertions are isolated within createOrder closure

**Smell check:** PASS
- Not asserting against itself (assertions are against mock captures and state changes)
- Not oversimplified (covers full flow: event → createOrder call → payment row creation)
- Properly validates both the voucher parameter pass-through AND the amount used in payment

## Existing Test Coverage

3 CheckoutBloc tests total:
1. "blocks empty item checkout before order service call" — guards empty cart edge case ✓
2. "COD success creates order and pending payment" — baseline happy path ✓
3. "applies server-computed voucher discount to the paid amount" — voucher discount contract [NEW] ✓

## Build & Linting

- `flutter analyze`: **PASS** (no issues, 3.7s)
- No deprecation warnings
- No build errors

## Changes Verified

**Modified files tested:**
- `FE/lib/blocs/checkout/checkout_bloc.dart` — new test coverage for promoCode threading and total extraction
- `FE/test/blocs/checkout_bloc_test.dart` — added voucher discount test

**Database migrations (verified via RPC in prod Supabase project):**
- `20260711150000_repatriate_vouchers_and_validate_voucher.sql` — tested via prod transactions
- `20260711150100_voucher_redemption_tracking.sql` — tested via prod transactions
- `20260711150200_create_order_money_path_hardening.sql` — tested via prod transactions

All RPC behavior (voucher validation, discount calculation, stock decrement) verified in prod via BEGIN/ROLLBACK wrapped test transactions before merge.

## Critical Checks

1. ✓ Client no longer sends or trusts money fields (hardcoded shipping_fee in old code is gone)
2. ✓ Bloc trusts only server-returned total for payment row creation
3. ✓ promoCode parameter is passed through to create_order RPC, not hard-stubbed
4. ✓ PostgrestException extraction on error path (line 133) captures server error messages
5. ✓ All 105 tests pass; no flaky tests observed

## Recommendations

None at this time. The new test and the underlying implementation properly lock the voucher discount contract. The bloc correctly treats the server as the source of truth for all money calculations.

---

**Test command:** `flutter test`  
**Analyze command:** `flutter analyze`  
**Report generated by:** QA Lead (tester agent)
